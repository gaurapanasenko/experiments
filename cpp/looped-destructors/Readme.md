# Shoot in head while destructing looped references

This experiments shows what happens when used references except weak references. Destructors will be running recursivery until stack will be overflowed.
